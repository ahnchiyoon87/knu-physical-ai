services:
  graph:
    image: neo4j:2026.08.1
    ports:
      - "127.0.0.1:7474:7474"
      - "127.0.0.1:7687:7687"
    environment:
      NEO4J_AUTH: "neo4j/${NEO4J_PASSWORD:?Set NEO4J_PASSWORD in your local .env}"
    volumes:
      - graph_data:/data
volumes:
  graph_data:
