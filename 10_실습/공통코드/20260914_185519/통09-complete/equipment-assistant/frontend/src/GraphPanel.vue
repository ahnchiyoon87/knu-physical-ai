<script setup>import {ref} from 'vue';import {session,request} from './api';const start=ref(''),end=ref('')</script>
<template><section class="panel"><p class="eyebrow">대상 → 관계 → 근거</p><h2>같은 번호가 같은 사건을 뜻하지는 않습니다</h2><button :disabled="session.busy" @click="request('/api/ontology/load',{profile:session.profile})">관계표 적재·대조</button><div class="form-row"><label>시작 대상<input v-model="start"/></label><label>끝 대상<input v-model="end"/></label><button :disabled="session.busy||!start||!end" @click="request('/api/ontology/path',{start,end})">관계 경로 찾기</button></div><p class="muted">찾은 경로는 관련성을 보여 줍니다. 실제 고장 원인을 확정하는 증거와 구별하세요.</p><button :disabled="session.busy" @click="request('/api/ontology/sync',{})">대기 중인 기록을 그래프에 반영</button></section></template>
