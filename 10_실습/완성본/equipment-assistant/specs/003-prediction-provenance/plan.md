# 연결

분류 직전에 읽은 원본 행과 lab.imports 메타데이터 → 순수 provenance 함수 → API artifact + graph_outbox 같은 트랜잭션 → 초기 관계표 조회의 저장 관계 합치기 / 이후 Neo4j 명시 동기화. 결과 화면은 실제 row_id와 lot_target을 표시한다. 새 모듈은 분류 시작 단계부터 패키지에 포함한다. DB·모델·GUI 실행 대신 순수 계약, import, Vue 빌드, 패키지 검사를 수행한다.
