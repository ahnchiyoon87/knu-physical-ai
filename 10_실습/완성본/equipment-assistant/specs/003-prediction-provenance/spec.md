# 분류 결과에서 원본 행으로 돌아간다

분류 평가 행의 원천·원본 행 번호·실제 LOT(없으면 null)를 저장한다. 적재 ID와 파일/매핑 해시를 결과에 연결한다. 결과→원본 행→LOT 관계를 같은 업무 트랜잭션의 그래프 대기에 저장한다. 분류 단계의 관계표 조회는 저장된 관계도 읽으며 Neo4j 단계는 명시 동기화 후 같은 ID 경로를 제공한다. 관련성은 인과가 아니다. 원천 없는 행, 중복 행, 평가 행이 원본에 없는 경우 오류로 멈춘다. 그래프 기록의 조회/반영과 전체 후속 파이프라인 자동 실행은 구별한다.
