# 책임

규칙 계산 중 실제 판정 카운터 → 감지 응답. PyOD 계산 결과 → 원본 행 결합 순수 함수 → 기존 artifact 저장/화면 전체 결과. LangGraph 노드의 입력/출력 예외 처리 → 중단 조건 → CLI 응답. 단계 코드 생성기의 규칙 함수와 provenance 의존성을 유지한다. 작은 계산/모의 게이트웨이 검사와 ZIP 구조만 실행한다.
