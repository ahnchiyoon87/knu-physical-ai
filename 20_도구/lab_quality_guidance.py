"""Quality/visual guidance at the point of use; no future rules dependency."""
QUALITY_ACTION = {
('quality',1): """**기준을 만들고 그 자리에서 적용합니다.** `mapping.json`의 선택 원천에서 `quality_rules`와 `clean_table`을 확인합니다. 참고 구조는 열 번호마다 근거(`provenance`)와 오류 코드(`error_codes`) 또는 하한/상한(`lower`/`upper`)을 둡니다. 오류 코드는 NULL로 읽고 범위를 벗어난 값은 표시하며 원본 행은 삭제하지 않습니다. 실제로 집계에서 제외할 조건은 별도로 정해야 합니다. 규칙이 없으면 임의 값을 채우지 말고 확인된 근거부터 준비합니다.

기준 변경을 저장한 뒤 서버를 다시 시작하고 품질 화면에서 ‘설정한 기준으로 정제 뷰 만들기’에 해당하는 동작을 요청합니다. 이어 ‘정제 뷰와 표시 사유’에서 원본 행 번호·변환 값·quality_flags를 나란히 읽습니다. 자기 화면의 버튼 이름이 다르면 같은 입력과 결과를 연결하면 됩니다.

정제 값을 다음 질문과 그래프에 사용하려면 같은 원천에 `"analysis_view": "clean"`을 지정하고 서버를 다시 시작합니다. JSON의 쉼표와 구조가 깨지지 않게 에이전트에게 해당 원천만 수정하도록 요청합니다. 정제 뷰를 만들기 전에 이 설정만 바꾸면 조회가 실패할 수 있습니다. `raw`로 되돌릴 수 있지만 실패를 숨기려고 자동 전환하지 않습니다. 기준을 바꿀 때는 원본의 총행 수, 표시된 행 수, 특정 열의 유효 값 수를 구분해 읽습니다.""",
('quality',2): """**오늘 추가하는 원천입니다.** 출발본에는 첫날 원천만 있으므로 [분류용 합성 표](../자료/품질/labeled.csv)와 [원천 등록 정보](../자료/품질/labeled_source.json)를 먼저 엽니다. 이 표의 라벨은 생성 규칙으로 만든 교육용 값이며 실제 제품 검사 결과가 아닙니다.

합성 표를 자기 프로젝트의 `data/generated/labeled.csv`로 복사합니다. 이미 그 경로에 자기 자료가 있으면 덮지 말고 새 이름으로 보존한 뒤 등록 경로를 맞춥니다. 에이전트에게 등록 JSON을 읽고 `mapping.json`의 `profiles → synthetic → sources` 아래 `labeled` 항목만 추가하도록 요청합니다. 기존 shots·conditions·worklog와 자기 수정은 유지합니다. 등록 정보의 path를 실제 복사 위치와 대조한 뒤 서버를 다시 시작합니다.

화면에서 synthetic / labeled를 선택해 적재한 뒤 품질 통계를 읽습니다. shots와 labeled의 품질을 비교하되 행 수·라벨 뜻·단위가 다른 값을 같은 분모로 합치지 않습니다. 원본을 사용한다면 이 합성 등록으로 대신하지 않고 해당 파일의 정의와 경로를 따로 등록합니다.""",
('quality',3): """**뜻 초안과 등록을 나눕니다.** 화면의 ‘설명서에서 열 뜻 초안 받기’에 열 번호와 실제 발췌 문장을 넣고 선택 모델로 요청합니다. 참고 구현은 초안을 사전에 자동 저장하지 않습니다. 사람이 인용과 뜻을 확인한 항목만 에이전트에게 해당 열 사전의 변경으로 요청하고 출처를 남깁니다. 다음 그래프를 보기 전에 원천 선택을 synthetic / shots 또는 자기 샷 원천으로 되돌립니다.""",
('visual',0): """**오늘 고른 보기를 유지합니다.** 앞에서 정제 뷰를 만들었다면 `analysis_view`가 선택한 값인지 확인하고 같은 LOT의 질문 결과와 그래프를 대조합니다. 합성 shots의 열 번호 22가 쿠션이며 LOT 4·5는 제공된 합성 설계의 비교 대상입니다. 가로축은 샷 순서입니다. 정제 뷰의 범위 표시는 값을 자동 제외한 결과가 아니므로 표시된 값이 선에 남아 있어도 설정을 먼저 읽습니다.

전체 곡선을 먼저 그린 뒤 관심 LOT와 구간으로 좁혀 원본 행을 확인합니다. 새 그래프를 별도 앱으로 만들지 말고 첫날의 원천·LOT 선택을 이어받게 요청합니다. 같은 프로젝트의 품질·질문·그래프가 서로 다른 원천을 읽고 있지 않은지 화면에 선택 범위를 남깁니다.""",
('visual',2): """**현재 진도에서 보고할 근거를 사용합니다.** 아직 규칙 감지를 만들기 전이므로 오늘은 선택한 원천의 원본 행 수·LOT 수와 앞에서 검토한 관찰을 보고합니다. 참고 보고서의 기본 건수는 원본 표에서 계산하므로 오류 코드를 NULL로 바꾼 정제 분석의 유효 값 수와 같지 않을 수 있습니다. 원본 전체 행·유효 값·의심 표시의 분모를 같은 숫자로 부르지 않습니다.

처음에는 ‘모델 설명 추가’를 끄고 같은 LOT 범위의 두 보고서를 읽습니다. 그다음 허용한 모델을 켜서 근거 번호가 있는 문장을 추가하고 각 숫자·주장을 근거와 대조합니다. 규칙 경보는 해당 기능을 만든 이후 보고서에 연결합니다. 지금 빈 경보 목록은 정상 확인이나 불량 0건의 증거가 아닙니다. 인쇄 전 현재 응답이 보고서인지 확인하고 ‘현재 화면 인쇄 / PDF 저장’으로 저장합니다."""
}
