# 00 · 이 묶음이 무엇인가 · 읽는 순서 · 확정 사항 · 뒤집힌 판정

**작성** 2026-09-09 · **갱신** 2026-09-12 · **정본** 이 묶음 하나. **실습 흐름의 정본은 `실습흐름_정본.html`** (설계서 03은 그 요약). 이전 산출물(경남대_재설계_핸드오프, 최종설계안_에이전트작업지시)은 전부 폐기.
**용도** 사람(유엔진)이 읽고 검토하는 핸드오프이자, 코드 에이전트에게 주는 작업지시서 원본.

## 1. 읽는 순서
`실습흐름_정본.html` → `설계서/01` 시나리오 → `02` 서비스·계약·스택 → `03` 회차 편성 → `04` 가이드 규격 → `05` 영상 → `06` 검사·완료판정 → `07` 작업 순서 → `뼈대초안/`(헌법·AGENTS 실물) → `리서치/`(근거 원문 요약 3편) → `참고/`.

## 2. 권위 순서 (위가 이긴다)
1. `참고/KNU_PhysicalAI_uEngine.xlsx` — 회차명·날짜·시수·영상·운영 규칙. 한 글자도 바꾸지 않는다.
2. 이 묶음 `설계서/` + `뼈대초안/`.
3. `리서치/` 3편 — 시나리오·현업 흐름·스택의 근거. 여기 없는 인과·동기·인물은 쓰지 않는다.
4. 유엔진 5계층 참조 아키텍처(대본) — 학생 서비스의 척추. 제품은 넣지 않고 개념만 학생 크기로.
5. 그 외 모든 이전 자산 — 권위 없음. 문장·파일 단위로만 재료.

## 3. 확정 사항 (논의 끝)
| # | 확정 |
|---|---|
| 1 | 두 반 같은 수준. 회차명이 같으면 같은 가이드 |
| 2 | 회차 = 4시간 = 출발 20분 + 블록 4개(50분) + 닫기 20분. 여유 0 |
| 3 | 이론은 영상. 현장은 실습. 사전준비 없음 — 설치는 특강·회차 안에서 |
| 4 | 비컴공 대학생. 손코딩 0. 첫 회차 바이브코딩, 실3(통2)부터 SDD(명세 먼저) |
| 5 | 가이드에 완성 프롬프트 0줄. 상황·문제·힌트·「됐는지」만(#22) |
| 6 | 하나의 서비스 「설비 이상 대응 어시스턴트」가 회차를 따라 자란다. 계속 온 학생은 자기 저장소를 이어간다. 출발 검사 5줄 실패 시에만 출발본 |
| 7 | git 필수, GitHub 원격 필수. 출발본 = 우리 저장소의 회차 태그 |
| 8 | 시나리오 = 실제 사출 현장의 문서화된 페인포인트 5 + 교대 일과 + 실물 양식 + KAMP 실데이터 위에 우리 서비스가 한 층씩 얹힘. 사건은 지어내지 않는다. 라벨: 근거 / 설계 / 합성 |
| 9 | 온톨로지·그래프가 첫날부터 척추 |
| 10 | 5계층 학생판. MCP는 일하는 에이전트(서비스 안) 전용, 도구 5개 |
| 11 | 스택: Supabase(로컬 Docker `supabase start`, 가입 없음 → 배포 회차에서 클라우드) = Postgres + pgvector + 대시보드 · 그래프DB 별도 · Docling · bge-m3 · RAGAS · LangGraph · Chronos-Bolt/PyOD · Anomalib · Cloud Run. **DuckDB·Chroma·LanceDB·Kùzu 제거. LiteLLM·Langfuse는 영상에서만** |
| 12 | 관측은 자체 `/trace`·`/health` 하나. 로그 한 파일·JSON 한 줄·고정 필드 |
| 13 | 스킬: 주는 것 `debug`·`spec` / 학생이 만드는 것 `leave-notes`(실3) → `ontology-extend`(실6) → `verify-property`(실7). 스크립트 3(`reload.py`·`eval.py`·`deploy.sh`) |
| 14 | 결과물은 학생마다 다르다. 판정은 「성질이 성립하나」. 파일명·함수명·수치를 가이드·검사·채점에 쓰지 않는다 |
| 15 | 코드 규범(원인 수정·조용한 폴백 금지(폴백은 쓰되 산출물에 남긴다)·주석은 코드 설명만) + 디버깅 7단계 + 영향도 배선 — `뼈대초안/AGENTS.md` |
| 16 | 인위적으로 깨뜨리는 절차 없음. 되돌리기는 자연히 한 번 겪게 둔다 |
| 17 | 공유 자원은 OpenAI 키 하나. 동시 다운로드 걱정 안 함 |
| 18 | 코드 에이전트·모델은 정해져 있음. 다루지 않음 |
| 19 | **과정의 목표는 활용 능력** — ① 시키기 ② 확인하기 ③ 이어 붙이기 ④ 한계 알기. 최종 목표는 「이 스택으로 주제가 바뀌어도 같은 것을 다시 만들 수 있는 사람」(PBL 연계). 회차 채점표는 이 넷이 열 |
| 20 | **어려운 인프라는 에이전트가 헌법대로 한다(와 포인트).** 학생 난이도는 모델·에이전트를 다루는 판단에 둔다. 가이드에 인프라 용어 상자를 두지 않고 「에이전트가 헌법대로 만든 것」 한 줄만 |
| 21 | **모델을 부르는 자리는 루프 여섯** — 날것으로 시킨다 → 질문 묶음으로 잰다 → 틀린 것을 종류로 가른다 → 장치 하나만 넣는다 → 다시 잰다 → 한 줄 남긴다. 재는 것 없이 만드는 블록은 없다 |
| 22 | **가이드 블록은 퀴즈** — 상황(실데이터) → 문제 → 「어떻게 시킬까?」 + 주석형 힌트 → 학생이 프롬프트 → 됐는지. 힌트는 끝까지 주되 회차가 갈수록 줄고 약해진다(실1·2 세 줄 → 실3~5 두 줄 → 실6·7 한 줄 → 실8·9 0~1). 시간으로 여는 힌트 없음. 강한 힌트·정답은 가이드에 없고 강사가 단톡방에서 — 그 문구도 가이드에 안 적는다 |
| 23 | **억지 페인포인트 금지.** 상황은 실데이터·도구가 실제로 하는 것에서만. 「대비」(나올 수 있는 것과 그때 한 걸음)는 강사용 파일에만, 학생 가이드에는 없다 |
| 24 | **장치 카드** — 고도화 장치는 카드 한 장(어떤 실패에/무엇을/언제/우리 데이터로 만드나/자사 제품 어디). 우리 데이터로 깨지는 것은 만들고(체크리스트 채점·정찰 SQL·LOT 앵커·번호로 받기·거절·표 행 자르기·하이브리드+리랭크·그래프 홉·상태 필터·도구 계약·가드레일·대화 상태), 안 깨지는 것은 「안다」(HyDE·다축·PRF·FK 확장·쿼리 캐시 fail-closed·값 매핑 존재 확인·응답 캐시 해시·exact-once). 카드는 `.agent/cards/` |
| 25 | **에이전트는 회차마다 노드 하나** — 실5 파이프라인 자동 실행 → 실6 그래프 길 → 실7 라우터 → 실8 잇기+도구 5+가드레일+승인+대화 상태. 8일차는 잇는 날 |
| 26 | **모델 둘** — API(GPT-5.4 mini) + 사내 GPU. config에서 읽고 화면에 어느 모델인지 항상 표시. 관측 탭에 토큰·비용. 실7에서 같은 질문셋을 둘로 돌려 점수·토큰·지연·비용. 조용한 폴백 금지 |
| 27 | **매일 세 줄** — 판단·한계·옮겨 보기(제조 아닌 데 쓰면 뭐가 바뀌나). 실9에 「다른 주제」 한 벌로 전이 검사. 발판은 뒤로 갈수록 학생에게(질문셋·맞음의 기준·장치 선택·명세·검사) |
| 28 | **시간을 이유로 내용을 줄이지 않는다.** 엑셀 회차 주제에 담긴 것을 전부, 자연스럽게, 바쁘게 |

## 4. 뒤집힌 판정 (이전 산출물에 남아 있으면 무시)
- 「12일 편성」「48H」 → 엑셀 21회차·19편.
- 「범위 8회차」 → 21회차 전부.
- 「전원 출발본에서 시작」 → 계속 온 학생은 자기 것. 출발 검사 실패 시만 출발본.
- 「대성정밀 IM-02 코어핀 사건」 → **폐기.** 근거 없는 창작. 시나리오 전면 교체(01).
- 「DuckDB 창구」 → Supabase. "옮기지 않는다"는 영상 개념으로만.
- 「MCP는 4일에 안 넣는다」 → 넣는다(일하는 에이전트 전용). 「만드는 에이전트에 MCP 붙이기」 → 안 붙인다.
- 「스킬 8종 / 4종 / kg-build·rag-eval·deploy 스킬」 → 스킬은 판단 절차만. 나머지는 스크립트.
- 「Langfuse·LiteLLM 실습」 → 영상으로.
- 「일부러 깨뜨리고 되돌리기」 → 제거.
- 「40명 동시에 버티나」 → 잣대 아님.
- 「사용자 카드 '엑셀이 느려서'」류 → 카드는 원문 페인포인트·교대 일과에서만.
- 「참이어야 하는 것을 블록 머리에 먼저 준다」 → 폐기. 규칙을 먼저 주면 학생은 규칙 맞추기를 한다. 부딪힌 뒤 학생이 시킨 말로.
- 「블록2 = 깨진 것을 본다(10분 상한)」 → 폐기. 깨짐은 채점표에서 학생이 찾는다. 시간 장치 없음.
- 「느리다 → 두 번 올라감 → LOT 800 → debug 첫 발동」 사슬(2026-09-11 초안) → 폐기. 실측 없는 추측 위에 쌓은 억지 페인포인트.
- 「부품 지도에 없는 열은 빈칸」 블록 → 폐기. 26열 중 25개가 AAS에 그대로 있어 성립 안 함.
- 「고정 SQL로 흔들림 해결」 → 폐기(회피). 체크리스트 채점·결정론 감점·최소 변경 수리로 잰 뒤 고정.
- 「LIMIT 강제」 → 폐기. SQL을 바꾸지 않고 실행 계층에서 행 제한(자사 정책).
- 「폴백 금지(전면)」 → 「조용한 폴백 금지」. 자사 두 제품이 폴백을 쓰되 warning을 남긴다.
- 「모델 하나」 → 둘.
- 「통3은 AI-Hub」 → KAMP 열화상(R5). 03·삽화설명서 §12에 남아 있던 것 정리.
- 「실1 카드 '0.2초 CSV'·'오늘 …'」 → 56번은 샷 단위·시각 없음. LOT 앵커.
- 「인프라 용어를 학생 가이드에서 설명」 → 안 한다. 에이전트가 한다.

## 5. 남은 확인 (사람이 하는 것, 설계를 막지 않음)
- KAMP 사출 실데이터 수령(계정) → `Reason` 값·드리프트 유무 확인 → 라벨 갱신.
- AI-Hub 자동차 부품 이미지 데이터의 이미지 수·라이선스 확인(통3).
- 통3·통5에서 「한 설비」 원칙이 깨지는 것 학교 고지.
- 영상 마스터 공유(19편 → 13편) 센터장 합의.
- 그래프DB 최종(FalkorDB vs Neo4j) — 첫 회차 완주 때 설치 무게로.
- KAMP 데이터를 학생 클라우드 Supabase에 올리는 것이 이용 조건에 걸리는지.
- 사내 GPU 모델(「OPUS 4.8」)의 서빙·컨텍스트 길이·JSON 스키마 출력·동시 40명 지연 실측.
- 실9 「다른 주제」 데이터 한 벌 선정(제조 아닌 표 하나·문서 몇 장·기준 하나).
- 자사 저장소 문서(text2sql prompts·config·README, Analyzer constitution·contract, 카탈로그 갱신 방식) 수령 → 장치 카드 본문 확정.
