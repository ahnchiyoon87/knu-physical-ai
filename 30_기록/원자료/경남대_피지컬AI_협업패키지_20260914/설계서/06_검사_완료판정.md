# 06 · 검사 · 완료 판정 (정본)

「됐다」는 검사 통과다. `60_검사/회귀검사.py`를 1단계에 만들고 모든 산출물을 통과시킨다. 결과는 `검사결과.md`(통과/실패/미실행).

## E · 편성·문서
E1 회차구성.json = 엑셀 2_오프라인일정 1:1 · E2 가이드 수 = 회차 수, 블록 4·출발·닫기·안 될 때 표 · E3 코드블록 안 「…해줘/만들어줘/보여줘/고쳐줘」 끝 줄 0(설치·서버 명령·경로 예외) · E4 본문에 mapping 값·합성 결함 값 0 · E5 금지 문구 0(손 든다·강사에게·단톡방·10분·제품명 4종) · E6 [영상 N 장면] 참조가 장면표에 존재 · E7 용어 첫 등장 6요소 · E8 캡처 수 = ✔ 수 ≥4, 폭 ≥1280 · **E9 「왜 오늘」·「상황」에 라벨(근거/설계/합성)과 출처 존재, 지어낸 상황 0** · **E10 가이드에 우리 완성본 파일명·함수명 참조 0** · **E11 블록 머리 `요구:` 줄 존재** · **E12 블록마다 「어떻게 시킬까?」+힌트 줄 수 ≤ 회차 상한(실1·2 3 / 실3~5 2 / 실6·7 1 / 실8·9 1)** · **E13 모델을 부르는 블록에 「잰다」(질문셋·채점표) 존재** · **E14 학생 가이드에 「대비」·「안 될 때」·단톡방·10분 문구 0** · **E15 인프라 용어 설명 0(마이그레이션·인덱스·HNSW·멱등·체크포인트 상자 없음), 「에이전트가 헌법대로 만든 것」 한 줄 존재** · **E16 닫기에 세 줄(판단·한계·옮겨 보기) 자리 존재**.

## C · 계약·코드 (우리 완성본용. 학생 저장소는 C4·C5·C6만)
C1 라우트 = constitution API 목록 · C2 열 이름·단위·정상범위·공차·역할명 리터럴 0 · C3 데이터 파일 복사·이동 0 · C4 거절·답변 실패·구판 필터·충분성 상태가 실제 호출로 성립 · C5 도구 5개 빈 결과=none, 잘못된 인자=error · C6 승인 우회 경로 0 · C7 외부 호스트 = OpenAI·배포 대상만 · C8 스킬 2 존재 + 규칙 5 + 메모리 3 · C9 금지 주석어 0(TODO/FIXME/HACK/XXX/임시/나중에/테스트용/회차/사용자 요청/이전 버전/workaround/temp) · C10 `except: pass`·빈 except·주석 처리 코드 블록 0 · C11 동일 본문 함수 0·미사용 import/함수 0·파일 머리 한 줄 · C12 검사 통과용 분기 참조 0 · **C13 로그 형식: 모든 줄에 rid/layer/step/status/loc, layer·step은 AGENTS 목록 안, print 0** · **C14 게이트웨이 밖 외부 호출 0** · **C15 스킬 본문에 파일 경로·함수명 리터럴 0, description에 발동 문구 예시 ≥6, 500행 미만** · **C16 모델 이름은 config에서만, 둘 이상 등록, 응답 meta에 model 필드** · **C17 로그에 model·tokens_in·tokens_out 필드(모델 호출 줄)** · **C18 폴백 경로가 있으면 meta.warnings에 남는다(조용한 폴백 0)** · **C19 모델이 만든 SQL은 실행 전 파싱 검사, 실행 계층 행 제한, SQL 본문 무변경**.

## D · 데이터
D1 mapping이 가리키는 파일·열 존재 · D2 작업일지 = 샷 수·검사 없는 LOT 0·미래 날짜 0 · D3 합성 결함이 실제 존재 + 합성 표시 파일 존재 · D4 공장문서에 결론 문장 0(원인은/때문에/미신고로 인해) · D5 강사용 온톨로지 정답표가 학생 출발본에 없음 · **D6 `data_check.py`가 블록별 판정을 낸다** · **D7 가이드 질문셋(실1 10·실6 20)이 완성본에서 실제로 돌아간 답과 함께 존재** · **D8 `.agent/cards/` 장치 카드 = 정본 HTML 표와 1:1** · **D9 `다른주제/` 한 벌 존재**.

## S · 출발본
S1 반×회차 태그 존재 · S2 태그 → 설치 → `supabase start` → 서버 → `/health` 200 → 프론트 200 자동, 소요 기록 · S3 첫 회차: 코드 0, 빈 파일 0, 껍데기 0, 필수 파일 존재, `.gitignore`에 .env·데이터·모델 · S4 CHANGES.md = 이전 태그와의 diff · S5 키·개인정보 0 · **S6 `/health`가 회차 번호별 전제 라우트 목록을 돌려준다**.

## V · 영상  V1 편 수·시간 합 4:00 · V2 과제 정의 + 검증 회차 출발 절에 그 과제 보는 문장.
## G · 채점  G1 남길 것 = 채점표 행 1:1 · G2 회차마다 네 축(시키기·확인하기·이어 붙이기·한계 알기) 열 존재 · G3 실9 「묶인 가정 목록」 자리 존재.

## 리허설 (사람 · 기록 형식은 우리가)
실1(=통1)·실7·실8(=통10)·통3·통9 다섯 회차 4시간 완주. 재는 것: 출발 20분 안에 화면(Docker·supabase start 포함)·블록별 소요·먼저 끝났으면 도달률·막힌 증상·첫 회차 시킨 횟수·학생 스킬 발동 여부(흐릿한 문장 10개). 시간을 이유로 내용을 줄이지 않는다(확정 #28). 리허설은 소요를 재는 것이지 뺄 것을 정하는 것이 아니다. 추가로 재는 것: 사내 모델 동시 40 지연 · 채점표가 실제로 갈리나(맞음/틀림/지어냄이 0이 아닌가).

## 완료 판정
검사 골격(전 항목 등록) → 데이터(D) → 완성본(C, 태그 21) → 출발본(S) → 가이드(E + 리허설 5) → 영상(V) → 채점(G). 「실측」「확정」은 검사 번호를 붙일 때만.
