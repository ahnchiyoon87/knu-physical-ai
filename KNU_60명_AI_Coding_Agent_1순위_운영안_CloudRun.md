# 경남대 60명 AI Coding Agent 운영안 — 1순위 권고안

## 1. 결론

이번 과정은 학생 60명이 단순 챗봇을 사용하는 것이 아니라, **SDD 기반 바이브코딩 방식으로 파일 읽기·수정, 터미널 실행, 테스트·디버깅, RAG, Agent, 배포까지 지속적으로 Coding Agent를 사용하는 과정**이다.

따라서 학생 개인이 각각 ChatGPT, Claude, Gemini 등을 결제하는 방식보다 아래 구조로 운영하는 것을 1순위로 권고한다.

> **유엔진 중앙결제 → LiteLLM Gateway → 학생별 Virtual Key / Quota → 동일 Coding Agent IDE**

핵심은 모델 계정을 학생에게 나눠주는 것이 아니라, **유엔진이 모델 API 비용을 중앙에서 결제하고 학생에게는 LiteLLM 가상키만 발급하는 것**이다.

---

## 2. 권고 아키텍처

```text
학생 60명
  │
  │ 학생별 Virtual Key
  ▼
OpenCode / Roo Code 등 동일 Coding Agent
  │
  ▼
GCP Cloud Run
  └─ LiteLLM Gateway
          ├─ RPM / TPM 제한
      ├─ 사용 가능 모델 제한
      ├─ 사용량 모니터링
      └─ Provider 장애 시 fallback
          │
          ├─ coding      → GPT-5.6 Luna
          └─ coding-high → Gemini 3.8 Flash 등 상위 모델
          │
          ▼
유엔진 법인 API 결제계정
```

학생 개인의 해외결제카드는 필요하지 않는다.

원본 OpenAI·Google 등의 API Key도 학생에게 배포하지 않는다.

---

## 3. LiteLLM 서버 배포 — GCP Cloud Run

현재 1순위 배포 방식은 **GCP Cloud Run에 LiteLLM Proxy를 컨테이너로 올리는 방식**으로 잡는다.

중요한 점은 **LLM 자체를 Cloud Run에 올리는 것이 아니라 LiteLLM이라는 가벼운 API Gateway만 실행**한다는 것이다. 실제 추론은 OpenAI, Google 등 외부 모델 API에서 수행하므로 GPU 서버가 필요하지 않는다.

### 권장 초기 설정

| 항목 | 권장값 |
|---|---|
| 배포 | **GCP Cloud Run** |
| Container | LiteLLM Proxy |
| Minimum instances | **0 (비용 테스트 단계)** |
| Maximum instances | 3~5부터 시작 후 조정 |
| CPU / Memory | 1 vCPU / 1~2GB부터 테스트 |
| 원본 API Key | Secret Manager 저장 |
| 학생별 Key | LiteLLM Virtual Key 60개 |
| DB | PostgreSQL 계열 사용 |
| 공개 Endpoint 예시 | `https://ai.uengine.org` |

### 비용 특성

Cloud Run은 Compute Engine VM처럼 서버를 켜둔 시간 전체에 고정 과금하는 방식이 아니다.

**Minimum instances = 0**으로 두면 요청이 없을 때 인스턴스가 0개까지 내려가므로, 테스트 기간에 사용하지 않는 시간의 서버 비용을 크게 줄일 수 있다.

단, 첫 요청에서는 컨테이너가 다시 올라오는 **cold start**가 발생할 수 있다. 비용 테스트 단계에서는 이를 허용하고, 실제 수업에서 체감 지연이 문제가 되면 다음 중 하나로 조정한다.

- 수업 시작 전 health-check 요청으로 warm-up
- Minimum instances를 1로 올려 항상 1개 인스턴스 유지

실제 전체 비용에서는 LiteLLM Cloud Run 비용보다 **모델 API 토큰 비용이 훨씬 큰 비중**을 차지할 것으로 본다.

### Cloud Run을 선택하는 이유

1. 테스트 단계에서 요청이 없을 때 비용을 최소화하기 쉽다.
2. 별도 VM 운영·패치 부담이 적다.
3. 60명이 동시에 요청할 때 자동 확장이 가능하다.
4. 이후 모델 Provider를 변경해도 학생의 IDE와 Virtual Key 구조는 유지할 수 있다.
5. GPU가 필요하지 않다.


## 4. 왜 이 방식인가

LiteLLM 외에도 OpenRouter, Cloudflare AI Gateway, Provider 직접 API 배포, Team 좌석 구매, 계정공유, GPU 직접 호스팅 등을 비교했다.

| 방식 | 비용 | 학생별 Quota | 모델 교체 | 관리 | 판단 |
|---|---:|---|---|---|---|
| **LiteLLM + Provider API** | **낮음** | **가능** | **자유로움** | 중 | **1순위** |
| OpenRouter 관리형 | 낮음~중 | 가능 | 자유로움 | 매우 편함 | 2순위 |
| Cloudflare AI Gateway | 낮음 | 가능 | 자유로움 | 편함 | 3순위 |
| Provider API 직접 배포 | 낮음 | 불편 | 어려움 | 불편 | 후순위 |
| Team/Business 60석 | 높음 | 좌석단위 | 제한적 | 편함 | 비용 과다 |
| 공유 구독계정 | 중 | 공용 한도 | 제한적 | 불편 | 충돌 위험 |
| GPU 직접 호스팅 | 매우 높음 | 직접관리 | 가능 | 매우 어려움 | 비경제적 |

이번 교육처럼 60명 각각의 사용량을 통제하고, 과금 사고를 막으면서 필요하면 모델을 바꿔야 하는 환경에서는 **중앙 Gateway 방식이 가장 적합**하다.

---

## 5. 기본 모델 — GPT-5.6 Luna

주요 Provider의 최신 저가·고성능 모델을 비교한 결과, 현재 기본 모델은 **GPT-5.6 Luna**가 가장 가성비가 좋다고 판단한다.

최신 세대의 경량 모델은 과거의 소형 모델과 달리 이전 세대 상위 모델 수준의 성능까지 올라오는 경우가 있다. Luna 역시 단순히 오래되거나 성능이 낮아서 싼 모델이 아니라, **최신 GPT-5.6 계열의 저비용·고용량 처리용 모델**이다.

### 모델 비교

학생 1명 전체 과정 사용량을 **20M input + 4M output**으로 가정한 중간 시나리오 기준.

| 모델 | API 단가 Input / Output (1M) | 코딩 성능 판단 | 학생 1명 예상 | 60명 예상 | 판단 |
|---|---:|---|---:|---:|---|
| **GPT-5.6 Luna** | **$0.20 / $1.20** | 상위 모델과 격차가 크지 않음 | **$8.8** | **약 $528** | **가성비 1위** |
| DeepSeek V4.1 Flash | 저가 | 최신 가성비 후보 | 재검증 필요 | 재검증 필요 | A/B 후보 |
| Grok Build | $1 / $2 | Coding Agent 특화 | 약 $28 | 약 $1,680 | Luna보다 비쌈 |
| Gemini 3.8 Flash | 약 $1.50 / $7.50 수준 | 상위 성능 | 약 $60 | 약 $3,600 | 고난도용 |
| GPT-5.6 Terra | $2 / $12 | Luna보다 강함 | 약 $88 | 약 $5,280 | 기본으로는 과비용 |

**Luna는 상위 모델 대비 가격 차이가 매우 큰 반면 코딩 성능 차이는 상대적으로 작아, 전체 요청을 비싼 모델로 처리할 필요가 없다.**

---

## 6. 권장 모델 사용 방식

- `coding` → **GPT-5.6 Luna**
- `coding-high` → **Gemini 3.8 Flash 등 상위 모델**

학생은 평소 Luna를 사용한다.

다음과 같은 경우에만 `coding-high`를 사용한다.

- Luna로 2회 이상 해결하지 못한 오류
- 복잡한 아키텍처 설계
- RAG/Agent 전체 구조 문제
- 장기 디버깅
- 큰 범위의 리팩터링

초기에는 자동 라우팅보다 **학생이 명시적으로 `coding-high`를 선택하는 방식**이 비용 관리에 더 안전하다.

---

## 7. 예상 비용

학생 1명 전체 과정:
- Input: 20M tokens
- Output: 4M tokens

예상 모델비:
- **Luna 100%:** 약 $528 / 60명
- **Luna 95% + 상위모델 5%:** 약 $680 전후 / 60명

환율과 결제수수료, LiteLLM 서버비까지 포함하면:

> **실제 목표 운영비: 약 110~155만원**

예상보다 사용량이 많을 수 있으므로 실제 확보 예산은:

> **약 200~250만원**

수준을 권장한다.

---

## 8. 학생별 Quota 권장

| 항목 | 권장값 |
|---|---:|
| 학생 1인 전체 과정 Hard Cap | **$20** |
| 경고 | $10 / $15 / $18 |
| 추가 승인 | +$5 단위 |
| 전체 기본 최대 노출액 | **$1,200** |
| 키 만료 | 과정 종료 직후 |
| 이상사용 알림 | 일 $2~3 이상 |
| 초기 RPM | 10~15 / 학생 |
| 초기 TPM | 0.5~1M / 학생 |

$20은 선결제 금액이 아니라 **학생이 최대 사용할 수 있는 상한**이다.

---

## 9. 운영 원칙

1. 학생에게 Provider 원본 API Key를 배포하지 않는다.
2. 학생별 Virtual Key를 별도로 발급한다.
3. 학생 이름 대신 교육용 ID(KNU-001~KNU-060)를 사용한다.
4. 학생별 사용금액과 토큰 사용량을 모니터링한다.
5. LiteLLM 서버 장애에 대비해 자동 재시작 또는 이중화를 둔다.
6. Provider 장애 시 다른 모델로 fallback할 수 있게 구성한다.
7. 과정 종료 직후 학생 Virtual Key를 전부 폐기한다.
8. Provider Master Key도 과정 종료 후 rotation한다.

---

## 10. 사전 검증

본수업 전에 반드시 소규모 파일럿을 진행한다.

- 5~10명
- 실제 수업과 동일한 프로젝트
- 3~4시간 연속 사용
- Luna 기본 사용
- 고난도 작업 일부 상위모델 사용

확인 항목:
- 학생 1명당 실제 토큰 사용량
- 학생 1명당 실제 비용
- 60명 동시 사용 시 Provider rate limit
- 429 오류 발생 여부
- 평균/p95 응답속도
- 긴 Context 사용량
- Prompt Cache 비율
- Luna로 해결하지 못하고 상위 모델이 필요한 비율

---

## 11. 최종 권고

### 1순위

> **유엔진 Provider API 중앙결제  
> → GCP Cloud Run의 LiteLLM Gateway  
> → 학생별 Virtual Key 60개  
> → OpenCode/Roo Code  
> → GPT-5.6 Luna 기본  
> → 고난도 작업만 상위 모델**

### 예산

- 모델비 예상: 약 **95만원 전후**
- LiteLLM/서버/결제비 포함 목표: **110~155만원**
- 안전 확보 예산: **200~250만원**

### 한 줄 결론

**60명에게 각각 구독계정을 구매하거나 공유시키는 방식보다, 유엔진이 API를 중앙 구매하고 LiteLLM에서 학생별 키와 Quota를 나눠주는 구조가 비용·관리·과금안전성 측면에서 가장 유리하다. 기본 모델은 GPT-5.6 Luna로 두고, 해결이 어려운 작업만 상위 모델로 올리는 방식이 현재 가장 가성비가 좋다.**
